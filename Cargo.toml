[package]
name = "tcp-brutal-auto"
version = "0.1.2"
edition = "2021"
description = "Watch TCP handshake peers and add TCP Brutal destination rules before connections establish"

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
strip = true
